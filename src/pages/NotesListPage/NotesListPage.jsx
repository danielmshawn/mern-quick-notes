

export default function OrderHistoryPage({ note }) {
  // async function handleCheckToken() {
  //   const expDate = await checkToken();
  //   console.log(expDate);
  
 
  
  return (
   <h2>{ note }</h2>
  )
  
  }